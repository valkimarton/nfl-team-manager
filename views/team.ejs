<%- include('header') -%>
<%- include('navbar') -%>

    <div class="container">
        <div class="row justify-content-md-center">
            <div class="col-10 col-lg-8">
                <a href=<%= "/team/edit/" + team._id %> class="btn btn-sm btn-primary" style="float: right; margin: 10px; font-size: 18px;">Edit</a>
                <h1><%= team.name %></h1>
                <br/>
                <form action="#">
                    <div class="form-group row">
                        <label for="estimated" class="control-label col-sm-2"><b>Estimated</b></label>
                        <label class="col-sm-10" id="estimated"><%= team.estimated %></label>
                    </div>
                    <div class="form-group row">
                        <label for="owner" class="control-label col-sm-2"><b>Owner</b></label>
                        <label class="col-sm-10" id="owner"><%= team.owner %></label>
                    </div>
                    <div class="form-group row">
                        <label for="coach" class="control-label col-sm-2"><b>Head coach</b></label>
                        <label class="col-sm-10" id="coach"><%= team.coach %></label>
                    </div>
                </form>
            </div>
        </div>
        <div class="row justify-content-md-center">
            <div class="col-10 col-lg-8">
                <br/><br/>
                <h5>Players</h5>
                <table class="table table-striped table-bordered">
                    <tr>
                        <th>
                            <b>Player name</b>
                        </th>
                        <th style="width:150px">
                            <b>Manage</b>
                        </th>
                    </tr>
                    <% players.forEach(function(player) { %>
                        <tr>
                            <td>
                                <a href=<%= "/player/" + player._team + "/" + player._id %> ><b><%= player.name %></b></a>
                            </td>
                            <td style="width:150px">
                                <a href=<%= "/player/" + player._team + "/" + player._id %> class="btn btn-sm btn-primary">View</a>
                                <a href=<%= "/player/" + player._team + "/del/" + player._id %> class="btn btn-sm btn-danger">Retire</a>
                            </td>
                        </tr>
                    <% }); %>
                </table>
                <br/>
                <a href=<%= "/player/" + team._id + "/new" %> class="btn btn-primary">New Player</a>
            </div>
        </div>  
    </div>

<%- include('footer') -%>